<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>CarHub - Reset Password</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <link href="~/img/favicon.ico" rel="icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@600;700&family=Ubuntu:wght@400;500&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="~/lib/animate/animate.min.css" rel="stylesheet">
    <link href="~/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/style.css" rel="stylesheet">
    <style>
        body {
            background: url('/img/pictureFYP.jpg');
            background-size: cover;
            min-height: 100vh;
            position: relative;
        }

            body::before {
                content: '';
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0,0,0,0.6);
                z-index: 0;
            }

        .container-xxl {
            position: relative;
            z-index: 1;
        }

        .back-to-top {
            display: none !important;
        }
    </style>
</head>

<body>
    <div id="spinner" class="show bg-white position-fixed translate-middle w-100 vh-100 top-50 start-50 d-flex align-items-center justify-content-center">
        <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
            <span class="sr-only">Loading...</span>
        </div>
    </div>

    <div class="container-xxl py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="bg-dark rounded h-100 d-flex flex-column justify-content-center text-center p-5 wow zoomIn" data-wow-delay="0.6s">
                        <h1 class="text-primary mb-4"><i class="fa fa-lock me-3"></i>Reset Password</h1>
                        <p class="text-white mb-4">Check your email for the 6-digit code</p>

                        @if (ViewBag.ErrorMessage != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                <i class="fa fa-exclamation-triangle me-2"></i>
                                @ViewBag.ErrorMessage
                            </div>
                        }

                        <form method="post" asp-action="ResetPassword" asp-controller="Auth">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="Email" value="@ViewBag.Email" />
                            <div class="row g-3">
                                <div class="col-12">
                                    <h5 class="text-white text-start mb-3"><i class="fa fa-shield-alt me-2"></i>Reset Code</h5>
                                </div>
                                <div class="col-12">
                                    <input type="text" class="form-control border-0 text-center"
                                           placeholder="Enter 6-digit code"
                                           name="ResetCode"
                                           required
                                           maxlength="6"
                                           pattern="\d{6}"
                                           style="height: 55px; font-size: 24px; letter-spacing: 10px;">
                                </div>

                                <div class="col-12 mt-4">
                                    <h5 class="text-white text-start mb-3"><i class="fa fa-key me-2"></i>New Password</h5>
                                </div>
                                <div class="col-12">
                                    <div class="input-group">
                                        <input type="password" class="form-control border-0"
                                               placeholder="New Password (min 6 characters)"
                                               name="NewPassword"
                                               id="ResetNewPassword"
                                               required minlength="6"
                                               style="height: 55px;">

                                        <span class="input-group-text bg-white"
                                              style="cursor:pointer;"
                                              id="toggleResetNewPassword">
                                            <i class="fa fa-eye" id="toggleResetNewPasswordIcon"></i>
                                        </span>
                                    </div>
                                </div>

                                <div class="col-12">
                                    <div class="input-group">
                                        <input type="password" class="form-control border-0"
                                               placeholder="Confirm New Password"
                                               name="ConfirmPassword"
                                               id="ResetConfirmPassword"
                                               required
                                               style="height: 55px;">

                                        <span class="input-group-text bg-white"
                                              style="cursor:pointer;"
                                              id="toggleResetConfirmPassword">
                                            <i class="fa fa-eye" id="toggleResetConfirmPasswordIcon"></i>
                                        </span>
                                    </div>
                                </div>


                                <div class="col-12 mt-4">
                                    <button class="btn btn-primary w-100 py-3" type="submit">
                                        <i class="fa fa-check me-2"></i>Reset Password
                                    </button>
                                </div>
                                <div class="col-12">
                                    <a href="@Url.Action("CustomerLogin", "Auth")" class="text-white">
                                        <i class="fa fa-arrow-left me-2"></i>Back to Login
                                    </a>
                                </div>
                                <div class="col-12">
                                    <button type="button" class="btn btn-outline-light" onclick="document.getElementById('resendResetForm').submit();">
                                        <i class="fa fa-redo me-2"></i>Resend Code
                                    </button>
                                </div>
                                <div class="col-12 mt-3">
                                    <p class="text-white-50 mb-0"><small>Code expires in 15 minutes</small></p>
                                </div>
                            </div>
                        </form>
                        <form id="resendResetForm" method="post" asp-action="ResendPasswordResetCode" asp-controller="Auth" style="display: none;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="Email" value="@ViewBag.Email" />
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/wow/wow.min.js"></script>
    <script src="~/js/main.js"></script>
    <script>
        // New Password Toggle
        const resetNewPassword = document.getElementById("ResetNewPassword");
        const toggleResetNewPassword = document.getElementById("toggleResetNewPassword");
        const toggleResetNewPasswordIcon = document.getElementById("toggleResetNewPasswordIcon");

        toggleResetNewPassword.addEventListener("click", () => {
            const type = resetNewPassword.getAttribute("type") === "password" ? "text" : "password";
            resetNewPassword.setAttribute("type", type);

            toggleResetNewPasswordIcon.classList.toggle("fa-eye");
            toggleResetNewPasswordIcon.classList.toggle("fa-eye-slash");
        });

        // Confirm Password Toggle
        const resetConfirmPassword = document.getElementById("ResetConfirmPassword");
        const toggleResetConfirmPassword = document.getElementById("toggleResetConfirmPassword");
        const toggleResetConfirmPasswordIcon = document.getElementById("toggleResetConfirmPasswordIcon");

        toggleResetConfirmPassword.addEventListener("click", () => {
            const type = resetConfirmPassword.getAttribute("type") === "password" ? "text" : "password";
            resetConfirmPassword.setAttribute("type", type);

            toggleResetConfirmPasswordIcon.classList.toggle("fa-eye");
            toggleResetConfirmPasswordIcon.classList.toggle("fa-eye-slash");
        });
    </script>

</body>

</html>